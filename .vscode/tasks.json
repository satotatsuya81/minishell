{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Build",
        "type": "shell",
        "command": "make re",
        "options": {"cwd": "${workspaceFolder}"},
        "group": {
          "kind": "build",
          "isDefault": true
        }
      }
    ]
  }
